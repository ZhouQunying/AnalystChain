# AnalystChain 开发快速卡

> **一眼看懂，立即行动**

---

## 🛠️ 3个工具

| 工具 | 用途 | 何时用 | 命令 |
|------|------|--------|------|
| `scripts/check_notebook_syntax.py` | 检查Notebook语法 | 编辑`.ipynb`后 | `python scripts/check_notebook_syntax.py <file>` |
| `setup.py` | 打包安装项目 | 需要`import analyst_chain`时 | `pip install -e .` |
| `tests/` | 自动化测试 | 修改代码后 | `python tests/test_*.py` |

---

## ✅ 何时测试？（一个原则：改什么测什么）

| 修改内容 | 必须运行的测试 |
|---------|---------------|
| 修改工具代码（`tools/`） | `python tests/test_工具名.py` |
| 修改Agent（`agents/`） | `python tests/test_agent名.py` |
| 修改Notebook | 1. 语法检查 2. 完整执行一次 |
| 重建知识库 | `python tests/test_knowledge_retrieval.py` |

---

## 📋 标准开发流程（5步）

```
1. 确认方案 → 2. 写代码 → 3. 检查语法 → 4. 运行测试 → 5. 提交
```

---

**就这么简单。**
